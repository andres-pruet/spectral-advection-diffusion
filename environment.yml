# create the environment from this file with `conda env create -f environment.yml`
name: sad
channels:
  - conda-forge
dependencies:
  - numpy
  - numba
  - scipy
  - matplotlib
  # - cupy ## this one breaks on macs. Users who don't have cupy installed will have to install it manually.