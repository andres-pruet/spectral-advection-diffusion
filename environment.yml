# create the environment from this file with `conda env create -f environment.yml`
name: sad
channels:
  - conda-forge
dependencies:
  - numpy
  - scipy
  # - time
  # - pickle
  - matplotlib
  # - datetime
  - cupy